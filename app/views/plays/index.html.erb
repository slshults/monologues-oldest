<h1>Listing plays</h1>

<table>
  <tr>
    <th>Author</th>
    <th>Title</th>
  </tr>

<% @plays.each do |play| %>
  <tr>
    <td><%=h play.author_id %></td>
    <td><%=h play.title %></td>
    <td><%= link_to 'Show', play %></td>
    <td><%= link_to 'Edit', edit_play_path(play) %></td>
    <td><%= link_to 'Destroy', play, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New play', new_play_path %>